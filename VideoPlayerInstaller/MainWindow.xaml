<Window x:Class="VideoPlayerInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VideoPlayerInstaller"
        xmlns:viewModel="clr-namespace:VideoPlayerInstaller.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="450"
        WindowStyle="None"
        Background="Transparent"
        ResizeMode="NoResize"
        AllowsTransparency="True">
    
    <Window.DataContext>
        <viewModel:MainWindowViewModel></viewModel:MainWindowViewModel>
    </Window.DataContext>
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    
    <Border CornerRadius="10" Background="Gray">
        <Grid MouseDown="UIElement_OnMouseDown">
            <StackPanel VerticalAlignment="Center" Margin="10">
                <Button Command="{Binding StartDownloadCommand}" Style="{StaticResource MaterialDesignFlatButton}" Height="30" Width="120" Content="Install"></Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>